# Midnight network to target: preview | preprod
VITE_NETWORK_ID=preprod

# Address of the deployed ClearLend contract on that network.
# Leave empty to auto-deploy a fresh instance on first wallet connect
# (the address is then printed to the browser console and cached locally).
VITE_CLEARLEND_CONTRACT_ADDRESS=
